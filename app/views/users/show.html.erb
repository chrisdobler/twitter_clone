<p id="notice"><%= notice %></p>

<h3><%= @user.username %></h3>

<% @tweets.each do |tweet| %>
	<p>
	<%=tweet.text if tweet.is_a?(Twitter::Tweet) %>
	</p>
<% end %> 

<%= link_to 'Edit', edit_user_path(@user) %> |
<%= link_to 'Back', users_path %>
